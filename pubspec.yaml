name: fieldshub
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  # --- Firebase (đã đồng bộ phiên bản major) ---
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_database: ^12.0.3
  firebase_storage: ^13.0.3

  # --- Thư viện khác ---
  mailer: ^6.6.0
  http: ^1.2.2
  fl_chart: ^1.1.1
  qr_flutter: ^4.1.0
  image_picker: ^1.1.2
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  open_file: ^3.5.10
  cached_network_image: ^3.3.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
